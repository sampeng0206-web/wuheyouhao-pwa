# 舞鶴 PWA 第四輪改版實作紀錄 (Implementation Notes)

本紀錄詳細記載本次第四輪改版（依用戶反饋 12 項修改）中，技術架構與需求對齊時的待釐清事項、決定、偏離與取捨。

---

## 1. 待釐清 (Clarifications)
*   **掃叭石柱說明文字之阿美語陳述**：
    *   **釐清點**：第十一項要求全站將「阿美族」改為「撒奇萊雅族」，但第四項掃叭石柱說明中提到 `Satokoay考古遺址為阿美語（石柱所在之地）`。
    *   **釐清結果**：經與您確認，此句為學術/地名歷史語言來源考證陳述，不受第十一項全站替換的限制，因此在第四項中精準保留「阿美語」，避免歷史事實被誤改。
*   **舞鶴木牌小卡片雙圖並列排版限制**：
    *   **釐清點**：手機版好物小卡片（寬度約 150px~170px）若採左右 50% 並列顯示雙圖，會導致木牌的雕刻細節與文字完全看不清楚，影響視覺觀感。
    *   **釐清結果**：經綜合考量，我們採用了「垂直堆疊（Stack）雙圖」的設計，在專門的 `.tag-img-wrapper` 內同時直向展示正面與反面兩張大圖，不僅細節保留完好，又避免了複雜的 JavaScript 點擊放大彈窗邏輯。

---

## 2. 決定 (Decisions)
*   **導覽列文字字級階層（長輩友善）**：
    *   **決定**：為了優化長輩閱讀體驗，「收藏、地圖、遊戲、好物」四個按鈕的字體大小從 `13px` 提升到 `16px` 並設定為 `font-weight: 700`；同時為了維持視覺上的主從關係，「今日舞鶴」Logo 字體大小同步從 `16px` 提升至 `22px`，在手機版也透過 media query 同步微調為 `14px` 與 `18px`，在增強清晰度的同時，兼顧排版的對稱性與精美感。
*   **翻牌畫面黑字與去除陰影**：
    *   **決定**：為了解決長輩反映的卡片背面文字模糊問題，將 `.card-fortune-title`、`.card-fortune-quote`、`.card-fortune-interpretation`、`.card-fortune-ending` 等卡片內所有橫書文字的 `color` 一律改為純黑色 (`#000000`)，並將 `text-shadow` 設為 `none`。這使文字與底圖 `crane_back_v2.png` 產生最高對比度，底圖浮水印（木雕花紋）被自然呈現在文字之下。
*   **縮短「心靈鶴湯」與解籤的間距**：
    *   **決定**：將卡片背面的 HTML 結構重組，將標題與籤詩歸入 `.card-top-group`，將分隔線、解籤內文、結尾詞歸入 `.card-bottom-group`。搭配 CSS 設定 `.card-bottom-group` 的 `margin-top: auto` 以及元素間極小 gap，使解籤內容自然下移並緊密貼合，拿掉了原先 `space-between` 所造成的多餘空白。

---

## 3. 偏離 (Deviations)
*   **全站「阿美族」替換之範圍限定**：
    *   **偏離**：第十一項指示將全站指涉「阿美族」族群認定錯誤之處改為「撒奇萊雅族」。
    *   **原因**：除了第四項掃叭石柱的「阿美語」考證維持原樣外，原本第二篇的 Kalala 篇包含「阿美族語中藤編籃子的意思」、「阿美族先民用月桃葉」等字樣，但因本輪修改已將 Kalala 篇「整篇替換」為「北回歸線標誌公園」，因此這些段落直接被整段移除，不需進行逐字替換；實際修改範圍為 JS 寶物列表中的「阿美族獵人的勇氣石」改為「撒奇萊雅族獵人的勇氣石」。

---

## 4. 取捨 (Trade-offs)
*   **九宮格循環收集與勳章累計設計**：
    *   **取捨**：舊版的九宮格是靜態 DOM（固定 9 格），收集到 9 種寶物後，第四天起尋寶若抽到新寶物會直接被 `localStorage` 排除或因 DOM 無法渲染而消失。
    *   **解決方式**：我們捨棄了硬編碼 9 格的做法，改為用 JavaScript 動態生成網格 DOM：
        1. 網格最少維持 9 格（若空缺以 `?` 佔位），當前收集長度若大於 9 則自適應擴充，徹底杜減寶物溢出消失的錯誤。
        2. 當玩家集齊 9 個不同的寶物時，彈出勳章視窗並提供「收下勳章並開啟新一輪」按鈕。點擊後會把 active 寶物備份寫入歷史圖鑑 `craneAllTimeCollection`，隨後清空當前收集包並使累計勳章數（🏅 x N）+1，重新開啟新一輪。這在完美保留歷史足跡的同時，又提供了一個能讓玩家重複遊玩且不漏掉新寶物的永續遊戲循環。
*   **Service Worker 與 App 快閃舊 Logo 版本版控**：
    *   **取捨**：為解決 Android 手機點擊啟動 PWA 時快閃舊 Logo 的快取問題，我們採取了雙重版本控制措施：
        1. 在 `manifest.json` 與 `index.html` 中的 Logo icon 路徑加上 query parameters (如 `icon-192.png?v=4` 及 `manifest.json?v=4`)，強制瀏覽器在偵測到 manifest 或 icon 版本更新時重建 WebAPK。
        2. 在 `sw.js` 的 activate 事件中加入 `self.clients.claim()`，並將快取版本升級為 `wuheyouhao-v15`，刪除廢棄的 7 張舊圖，加入 14 張新圖，確保快取資源第一時間接管，排除舊版本渲染。

---

# 舞鶴 PWA 第五輪改版實作紀錄 (Implementation Notes - 5th Revision)

本紀錄詳細記載第五輪改版（依用戶反饋 6 項修改）中，技術架構與需求對齊時的待釐清事項、決定、偏離與取捨。

---

## 1. 待釐清 (Clarifications)
*   **籤詩第 38 首錯字修正細節**：
    *   **釐清點**：確認 `data/fortunes.json` ID 38 原始文字中「打不倒你的是」刪除單字「不」後的整體語意完整性。
    *   **釐清結果**：刪除「不」後改為「打倒你的是失去信念」，語意精準通順，且不影響籤詩其他段落結構。

---

## 2. 決定 (Decisions)
*   **【歸鄉告祖靈書】與「舞鶴木牌」的 HTML 換行與排版樣式**：
    *   **決定**：為了保留 19 句詩文與木牌敘述的完美分行呈現，內文採用 HTML 換行符號並賦予 `white-space: pre-line;` 樣式。這避免了在每個句尾使用大量 `<br>` 標籤導致 HTML 冗長，同時確保手機版呈現清晰易讀。
*   **瑞穗牧場多頁繪本 Slider 相容性**：
    *   **決定**：獨立文章「瑞穗牧場」採用與「掃叭石柱」完全相同的 `.pb-slider-article` DOM 結構（4 頁 `slider-page` + 4 個 `dot`），直接相容 `js/app.js` 的 `setupSlidingBook()` 滑動與提示點動態切換邏輯，不需撰寫額外 JavaScript 代碼。

---

## 3. 偏離 (Deviations)
*   **無偏離事項**：
    *   **原因**：本次 6 項反饋要求明確且均有對應圖片與資料支持，所有修改均與原始描述與現有系統 100% 契合，無需偏離需求。

---

## 4. 取捨 (Trade-offs)
*   **Service Worker 快取版本號與離線資源調配 (v16 -> v17)**：
    *   **取捨**：舊圖 `map_sakizaya_kispring.jpg` 被替代，且新增了 6 張高清圖片（`map_kiispring_new_1.png` ~ `2.png`、`map_ruisuifarm_1.png` ~ `4.png`）。
    *   **解決方式**：將 Service Worker 的 `CACHE_NAME` 從 `wuheyouhao-v16` 正式提升為 `wuheyouhao-v17`，移除廢棄快取並將 6 張新圖納入 `urlsToCache`，避免 PWA 載入無效舊圖或發生離線圖片無法讀取的情形。

---

# 舞鶴 PWA 第六輪（最終）改版實作紀錄 (Implementation Notes - 6th Revision)

本紀錄詳細記載第六輪改版（2 項文字微調）中，技術架構與需求對齊時的決定與取捨。

---

## 1. 待釐清 (Clarifications)
*   **無待釐清事項**：本次為純文字修改，需求清晰明確。

---

## 2. 決定 (Decisions)
*   **「現有可銷售商品」修改為「現有可訂購商品」**：
    *   **決定**：僅微調標題文字中的「銷售」二字為「訂購」，並一併更新 HTML 註解以保持程式碼的一致性。樣式、版面配置與其餘文字完全維持不變。
*   **「舞鶴木牌」狀態文字符號精簡**：
    *   **決定**：將原先「✽✽✽ 開放預購 ✽✽✽」（左右各三顆星號）精簡為「✽ 開放預購 ✽」（左右各一顆星號）。此變更可以有效減少行動裝置小螢幕上因版面寬度不足而造成的擠壓或換行問題，提升視覺體驗。

---

## 3. 偏離 (Deviations)
*   **無偏離事項**。

---

## 4. 取捨 (Trade-offs)
*   **Service Worker 快取版本號升級 (v17 -> v18)**：
    *   **取捨**：由於修改了 `index.html` 內文，為確保 PWA 應用程式在離線或線上模式下能即時載入最新的首頁文字，我們將 Service Worker 的 `CACHE_NAME` 從 `wuheyouhao-v17` 提升為 `wuheyouhao-v18`。這能強制瀏覽器在更新 SW 後清除舊快取並重新載入最新的 `index.html`，避免文字顯示舊內容的快取問題。

---

# 舞鶴 PWA 第七輪改版實作紀錄 (Implementation Notes - 7th Revision)

本紀錄詳細記載第七輪改版（2 項 Bug 修正 + 3 處圖片更新）中，技術架構與需求對齊時的待釐清事項、決定、偏離與取捨。

---

## 1. 待釐清 (Clarifications)
*   **九宮格尋寶第 3 抽重複問題的成因**：
    *   **釐清結果**：經查證，底層的尋寶隨機抽選使用 `Math.random()` 且無固定種子，為真正的隨機抽選。問題的根本原因在於，當 `huntCount` 累計自增至 3 時，自定義的事件處理函式在同一個微任務週期中，將 `toast.textContent` 從隨機獲取的寶物名稱瞬間覆蓋為固定的提示 `"今天的寶藏都找到了！明天再來 🕊"`。這使得瀏覽器來不及渲染中間的寶物資訊，進而給用戶造成了「每次第 3 抽都是同一內容」且「抽不出東西」的錯覺。修正方式為將二者文字內容合併。

---

## 2. 決定 (Decisions)
*   **馬立雲部落新增照片標題與 alt 屬性設計**：
    *   **決定**：對於馬立雲篇新增的集會所與天主堂圖片，決定將在圖片下方使用 `<p class="slider-content">` 分別顯示說明文字「部落集會所」與「天主堂」，並將其設置為對應圖片的 `alt` 屬性，確保語意完整。
*   **古伊之泉新增圖片之排版間距與位置**：
    *   **決定**：決定將 `map_kiispring_new_3.jpg` 與 `map_kiispring_new_4.jpg` 接續放置在現有第 2 張照片 `map_kiispring_new_2.png` 之下（添加 `style="margin-top: 12px;"` 以維持與其他大圖一致的間隔），並放在文章結尾可愛文案「需要我帶路嗎？...」之前，保持閱讀體驗的流暢。
*   **電腦版 Slider 指針樣式**：
    *   **決定**：為了提供電腦版桌面瀏覽器更直覺的交互指示，在 `css/style.css` 中將 `.slider-dots .dot` 加上 `cursor: pointer;` 屬性，引導用戶進行點擊。

---

## 3. 偏離 (Deviations)
*   **無偏離事項**。
    *   **原因**：本次修改完全貼合原始需求與設計意圖，無偏離或妥協處。

---

## 4. 取捨 (Trade-offs)
*   **保留實體舊圖片以作備份**：
    *   **取捨**：將被替代的舊圖片（`map_saoba_2.jpg`, `map_saoba_3.jpg`, `image_5.png` 等）從 HTML 結構及 Service Worker 的 `urlsToCache` 快取中安全移出，但為了避免物理刪除導致的歷史相依性問題或未來的回滾阻礙，我們選擇將這些檔案保留在 `images/` 目錄中，不做實體刪除。
*   **Service Worker 快取版本號升級 (v19)**：
    *   **取捨**：由於地圖圖片內容、HTML 結構及 JS 邏輯均有重要變動，為了確保使用者端（特別是離線 PWA 模式）能順利加載全新圖片並運行修正後的隨機/滑動邏輯，我們將快取版本升級為 `wuheyouhao-v19`，並為 HTML 中引用的 CSS 與 JS 檔案後方追加 `?v=5` 版本號，強迫瀏覽器捨棄舊版快取並完成熱更新。

---

# 舞鶴 PWA 第八輪改版實作紀錄 (Implementation Notes - 8th Revision)

本紀錄詳細記載第八輪改版（古伊之泉篇重整為滑動繪本 + 香積園篇照片擴充）中，技術架構與需求對齊時的待釐清事項、決定、偏離與取捨。

---

## 1. 待釐清 (Clarifications)
*   **無待釐清事項**：本次需求描述具體，新增照片檔名與排序邏輯皆非常清晰，無任何模糊空間。

---

## 2. 決定 (Decisions)
*   **古伊之泉與碑 Slider 結構與排版參數**：
    *   **決定**：將該篇文章的容器改為 `.pb-slider-article`，包含 `.slider-viewport` 與 `.slider-container`，並配置 5 個 `.slider-page` 頁面與 5 個指示點。容器寬度設為 `500%`，每頁寬度設為 `20%`。
    *   **排版調整**：
        1. 第一頁：展示 `map_kiispring_new_1.png` 與原本的【歸鄉告祖靈書】全文，其 `<p>` 標籤額外賦予 `white-space: pre-line;` 樣式，以保證 19 句詩文能夠忠實保留換行格式。
        2. 第二、三、四、五頁：按指定順序排列相片，底下新增對應的單行文字說明。
*   **香積園四張照片垂直堆疊排版**：
    *   **決定**：因前兩張照片並無下方文字說明，為保持視覺一致性，新增的四張照片（`map_xiangji_3.jpg` 至 `map_xiangji_6.jpg`）直接以垂直堆疊方式接在 `map_xiangji_2.jpg` 下方，不添加額外說明。每張照片均加上 `style="margin-top: 12px;"`，以維持均勻的間距，並配合現有照片加上對應的 `alt` 屬性。

---

## 3. 偏離 (Deviations)
*   **無偏離事項**。
    *   **原因**：本次實作細節與使用者指定的需求 100% 契合，無任何偏離原始要求的情形。

---

## 4. 取捨 (Trade-offs)
*   **Service Worker 快取升級與離線資源更新 (v19 -> v20)**：
    *   **取捨**：由於新增了 5 張新圖，為了避免 PWA 在離線使用時這些圖片出現破損（破圖），我們必須將 `sw.js` 中的 `urlsToCache` 快取名單同步擴充，加入新圖路徑。
    *   **快取清除**：為了強制瀏覽器清除舊有的快取並套用最新的 HTML 與圖片資源，將 `sw.js` 中的 `CACHE_NAME` 從 `wuheyouhao-v19` 升級為 `wuheyouhao-v20`，並將 `index.html` 內的 CSS/JS 引用版本參數升級為 `?v=6`，完成了完整的無痛更新循環。

---

# 舞鶴 PWA 第九輪改版實作紀錄 (Implementation Notes - 9th Revision)

本紀錄詳細記載第九輪改版（新增獨立「聯絡我們」篇章與區塊）中，技術架構與需求對齊時的待釐清事項、決定、偏離與取捨。

---

## 1. 待釐清 (Clarifications)
*   **無待釐清事項**：本次改版需求（地圖頁左右滑動、好物頁垂直堆疊、採用 3 張指定圖片且不加下方文字說明）非常具體且清楚，無任何模糊空間。

---

## 2. 決定 (Decisions)
*   **地圖頁「聯絡我們」Slider 結構與排版參數**：
    *   **決定**：本篇為第 8 篇，採用與其他多圖篇章一樣的 `.pb-slider-article` DOM 結構。容器內設置 `3` 個 `slider-page` 頁面與 `3` 個指示點。容器寬度設為 `300%`，每頁寬度設為 `33.333%`。
    *   **相片順序與無文案處理**：依序排列 3 張指定圖片，且不放置任何 `<p class="slider-content">` 或說明文字，確保版面乾淨。
*   **好物頁「聯絡我們」垂直堆疊樣式**：
    *   **決定**：將其作為好物頁的第 4 個 `ad-section` 放置於「規劃中商品」之後。標題比照第一項設定為「聯絡我們」，不配置右上角的徽章以簡化版面。
    *   **圖片樣式美化**：在 CSS 中為圖片建立專門的 `.contact-container` 與 `.contact-img` 樣式，設定 100% 寬度、8px 圓角、極細金色系邊框（`rgba(184, 134, 11, 0.08)`）與微弱陰影，使其與商品小卡片視覺風格完美融合，避免圖片突兀。

---

## 3. 偏離 (Deviations)
*   **無偏離事項**。
    *   **原因**：本次實作完美契合需求，未作任何偏離。

---

## 4. 取捨 (Trade-offs)
*   **Service Worker 快取版本號升級與離線快取更新 (v20 -> v21)**：
    *   **取捨**：為使離線/PWA 模式能順利加載 3 張全新的聯絡我們圖片，並即時清除快取以啟用最新的排版樣式，我們將 `sw.js` 內的快取名稱 `CACHE_NAME` 升級至 `wuheyouhao-v21`。
    *   **快取清除**：將 `index.html` 內引用的 CSS/JS 快取控制後綴升級至 `?v=7`，SW 註冊後綴也相應升級至 `?v=6`，提供穩定的快取清除與更新機制。

---

# 舞鶴 PWA 第十輪改版實作紀錄 (Implementation Notes - 10th Revision)

本紀錄詳細記載第十輪改版（修正圖片溢出 Bug、地圖聯絡我們/香積園 slider 重整、好物頁聯絡我們調整）中，技術架構與需求對齊時的待釐清事項、決定、偏離與取捨。

---

## 1. 待釐清 (Clarifications)
*   **無待釐清事項**：本次改動之各項內容（包含滑動格式與圖片歸屬搬移）皆非常明確，無任何模糊空間。

---

## 2. 決定 (Decisions)
*   **滑動圖片最大寬度與 object-fit 的優化**：
    *   **決定**：將 `.slider-img` 樣式設定加入 `max-width: 100%`，並將原有的 `object-fit: cover` 變更為 `object-fit: contain`，以此保證所有滑動繪本格式（包含瑞穗鄉、掃叭石柱、馬立雲部落、古伊之泉、瑞穗牧場、香積園、聯絡我們等）中的圖片，尤其是寬幅或有大量文字的圖片（例如聯絡我們卡片與 QR Code）在手機等窄屏裝置上能完整顯示，完全不被裁切且不溢出。
*   **「香積園」改為 Slider 格式與「撒奇萊雅鶴」歸屬調整**：
    *   **決定**：將香積園篇章轉換為 `.pb-slider-article` 滑動架構，設定總寬度為 `700%`，每一頁 `slider-page` 寬度為 `14.2857%`。第一頁為原創景色圖搭配 `<p class="slider-content">` 呈現的文字說明，第 2 至 6 頁放置香積園景色，第 7 頁則放入自「聯絡我們」移出的品牌圖片 `sakizaya_crane_brand.png`。
*   **「聯絡我們」地圖篇與好物頁同步去鶴圖**：
    *   **決定**：
        1. 地圖「聯絡我們」滑動篇章移除 `sakizaya_crane_brand.png`，調整為 2 頁，容器寬度改為 `200%`，每頁寬度 `50%`，提示圓點同步縮減為 2 個。
        2. 好物頁「聯絡我們」區塊移除最上方的 `sakizaya_crane_brand.png`，保留 2 張圖，垂直堆疊的呈現方式不變。

---

## 3. 偏離 (Deviations)
*   **無偏離事項**。
    *   **原因**：本次實作細節與您指定的需求 100% 契合，無任何偏離原始要求的情形。

---

## 4. 取捨 (Trade-offs)
*   **保留 slider-img 4:3 比例以維持排版穩定性**：
    *   **取捨**：將 `object-fit` 調整為 `contain` 後，如果圖片的原始寬高比不為 4:3，會使圖片在 4:3 容器內四周產生留白（但完全不裁切）。為了確保所有 slider 文章在左右滑動時，其容器高度一致而不發生抖動或排版坍塌，我們依然保留了 `.slider-img` 的 `aspect-ratio: 4 / 3`，而不去強制使高度自適應，這在版面穩定度與圖片完整性之間取得了最佳平衡。
*   **Service Worker 快取版本號升級與離線快取更新 (v21 -> v22)**：
    *   **取捨**：由於修改了 `index.html` 的 HTML 結構、CSS 樣式以及快取資源的分配，我們將 `sw.js` 中的 `CACHE_NAME` 升級為 `wuheyouhao-v22`，且 `index.html` 中的 CSS 與 JS 載入後綴同步提升至 `?v=8`，`sw.js` 載入後綴升至 `?v=7`，強制用戶端瀏覽器更新，避免使用到已快取的舊版佈局。
